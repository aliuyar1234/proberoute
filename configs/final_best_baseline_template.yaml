# This template is not run directly.
# `src.cli.select_finalist` must clone the selected screening baseline config,
# apply these canonical overrides, and write the result to
# `configs/generated/final_best_baseline_1b.yaml`.

project:
  exp_id: FINAL_BEST_BASELINE_1B
  stage: final
  priority: must
  notes: "Autogenerated from screening baseline selection. Do not hand edit the emitted file."

data:
  train_token_quota: 50000000
  val_token_quota: 5000000
  test_token_quota: 5000000

train:
  eval_every_steps: 1000
  save_every_steps: 2000
