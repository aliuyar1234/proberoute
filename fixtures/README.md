# Fixtures

These fixtures exist only for offline smoke tests and local integration tests.

## Files
- `tiny_corpus.jsonl`: newline-delimited JSON with a single `text` field per row.

## Rules
- Do not use these fixtures for the real scientific results.
- Do not report metrics from the fixture corpus in the paper.
- The smoke path must be able to run with networking disabled.
